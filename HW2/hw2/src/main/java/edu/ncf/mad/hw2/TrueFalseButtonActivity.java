package edu.ncf.mad.hw2;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;

public class TrueFalseButtonActivity extends AppCompatActivity {

    // Start with this activity first
    // It should have at least:
    // 1- a textview for the question
    // next and previous buttons to move between questions
    // true and false buttons
    // I am giving you the code for this activity, it is in a pdf
    // find it and type it in by hand.

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_true_false_button);
    }
}
